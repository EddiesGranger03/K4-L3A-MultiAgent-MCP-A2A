[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "day09-l3a-student-agent"
version = "0.1.0"
description = "Starter kit for the Day09 L3A Multi-Agent MCP + A2A competition"
requires-python = ">=3.11"
dependencies = [
    "httpx2>=2,<3",
    "jsonschema[format]>=4.25,<5",
    "mcp>=2,<3",
    "python-dotenv>=1.1,<2",
]

[project.optional-dependencies]
dev = ["pytest>=8.4,<9", "ruff>=0.12,<1"]

[project.scripts]
day09 = "student_agent.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# Kích hoạt chế độ tự động cho pytest-asyncio:
# Khi asyncio_mode = "auto", tất cả async def test_*() sẽ tự động được nhận diện
# và chạy bởi plugin pytest-asyncio mà không cần decorator @pytest.mark.asyncio
asyncio_mode = "auto"


[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
